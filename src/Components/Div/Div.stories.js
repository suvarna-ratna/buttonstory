import React from 'react';
import { Div } from './Div';

export default {
    title: "components/Div",
    Component: Div,
    argTypes: {backgroundColor: {control: 'color'}}
}

export const RedBtn = {
    backgroundColor: "red"
}