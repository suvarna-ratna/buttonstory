import React from 'react'

export default function Button({backgroundColor}) {
    return (
        <div>
            <button style={{backgroundColor}}>click for storybook</button>
        </div>
    )
}
